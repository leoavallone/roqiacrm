services:
  mongo:
    image: mongo:7
    container_name: roqiacrm-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - roqiacrm-mongo-data:/data/db

volumes:
  roqiacrm-mongo-data:
